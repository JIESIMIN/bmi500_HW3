Coding assistant: Claude Code running as the VS Code extension.
LLM: Claude Opus 5 (1M)
Model ID: claude-opus-5[1m]


## LLM Prompt 1

**Prompt**:
Read @matvec_multiply.py and implement the three following functions with comments:
1. A function that compute the dot product of two vectors using a for loop.
2. A function that compute the matrix-vector product using the dot_product function.
3. A main function that tests the matrix-vector product function using randomly generated data of size 1000x1000.

Keep code as concise and possible and prevent excessive function annotations.

**Response**:
Read matvec_multiply.py
Write matvec_multiply.py  (failed: file had changed since last read)
Write concise-code-style.md  (memory note)
Write MEMORY.md
Read matvec_multiply.py

The file was reset to the template, so I'll fill it in under your existing prompt comments.

Write matvec_multiply.py

import random
import time

# create a function to compute the dot product of two vectors using a for loop
# add comments for the selected function
def dot_product(a, b):
    # Sum of element-wise products; vectors must be the same length
    assert len(a) == len(b), "vectors must have the same length"
    result = 0
    for i in range(len(a)):
        result += a[i] * b[i]
    return result


# create a function to compute the matrix-vector product using the dot_product function
# add comments for the selected function
def matvec_multiply(A, x):
    # Each entry of the result is the dot product of one row of A with x
    return [dot_product(row, x) for row in A]


# create a main function to test the matrix-vector product function using randomly generated data of size 1000x1000
# add comments for the selected function
def main():
    # Random 1000x1000 matrix and length-1000 vector
    n = 1000
    A = [[random.random() for _ in range(n)] for _ in range(n)]
    x = [random.random() for _ in range(n)]

    # Time the product and show a sample of the result
    start = time.time()
    y = matvec_multiply(A, x)
    print(f"Output length: {len(y)}, time: {time.time() - start:.3f}s")
    print(f"First 5 entries: {y[:5]}")


if __name__ == "__main__":
    main()

I rewrote matvec_multiply.py to be short, with one-line comments. I didn't run it because you stopped the earlier run.

- dot_product(a, b): checks that the two vectors are the same length, then adds up a[i] * b[i] in a for loop.
- matvec_multiply(A, x): makes a list of dot_product(row, x), one for each row of A.
- main(): builds a random 1000x1000 matrix and a random vector of length 1000, times the product, and prints how long the result is plus its first 5 entries.

You had left import time in the file, so main() uses it for the timing. Your original prompt comments are still above each function. You can run it with python3 matvec_multiply.py.

I also saved a note to memory that you want short code without long docstrings.


## LLM Prompt 2

**Prompt**:
Come up with tests that can test the matrix-vector-product function and dot-product function in matvec_multiply.py.

**Response**:
Bash: list project files and show matvec_tests.py, requirements.txt, matvec_multiply.py
Write matvec_tests.py  (rejected by user, reason: "past the output in the conversation to me here")

Here are the tests for matvec_tests.py. I haven't written them to the file or run them. You can run them with python3 matvec_tests.py, or with pytest matvec_tests.py if pytest is installed.

import math
import random

from matvec_multiply import dot_product, matvec_multiply


# Check that calling fn(*args) raises an AssertionError
def raises_assertion(fn, *args):
    try:
        fn(*args)
    except AssertionError:
        return True
    return False


# generate tests for matrix-vector-product function in matvec_multiply.py
def test_matvec_known_values():
    assert matvec_multiply([[1, 2], [3, 4]], [5, 6]) == [17, 39]


def test_matvec_identity():
    # Identity matrix returns the vector unchanged
    x = [3, -1, 7]
    I = [[int(i == j) for j in range(3)] for i in range(3)]
    assert matvec_multiply(I, x) == x


def test_matvec_non_square():
    # 2x3 matrix times length-3 vector gives length-2 vector
    assert matvec_multiply([[1, 0, 2], [0, 1, -1]], [1, 2, 3]) == [7, -1]


def test_matvec_zero_vector():
    assert matvec_multiply([[1, 2], [3, 4]], [0, 0]) == [0, 0]


def test_matvec_dimension_mismatch():
    # Number of columns must equal vector length
    assert raises_assertion(matvec_multiply, [[1, 2], [3, 4]], [1, 2, 3])


def test_matvec_random_linearity():
    # A(x + z) should equal Ax + Az
    n = 50
    A = [[random.random() for _ in range(n)] for _ in range(n)]
    x = [random.random() for _ in range(n)]
    z = [random.random() for _ in range(n)]
    lhs = matvec_multiply(A, [xi + zi for xi, zi in zip(x, z)])
    rhs = [a + b for a, b in zip(matvec_multiply(A, x), matvec_multiply(A, z))]
    assert all(math.isclose(l, r) for l, r in zip(lhs, rhs))


# generate tests for dot-product function in matvec_multiply.py
def test_dot_known_values():
    assert dot_product([1, 2, 3], [4, 5, 6]) == 32


def test_dot_orthogonal():
    assert dot_product([1, 0], [0, 1]) == 0


def test_dot_negative_and_float():
    assert math.isclose(dot_product([-1.5, 2.0], [2.0, -0.5]), -4.0)


def test_dot_commutative():
    a = [random.random() for _ in range(100)]
    b = [random.random() for _ in range(100)]
    assert math.isclose(dot_product(a, b), dot_product(b, a))


def test_dot_empty():
    assert dot_product([], []) == 0


def test_dot_length_mismatch():
    assert raises_assertion(dot_product, [1, 2], [1, 2, 3])


# Run all tests without pytest
if __name__ == "__main__":
    tests = [f for name, f in dict(globals()).items() if name.startswith("test_")]
    for test in tests:
        test()
        print(f"PASS {test.__name__}")
    print(f"All {len(tests)} tests passed")

What the tests check:
- Matrix-vector product:
  - A small example you can check by hand.
  - The identity matrix returns the vector unchanged.
  - A non-square 2x3 matrix works.
  - A zero vector gives all zeros.
  - Mismatched sizes raise an error.
  - On random data, A(x+z) equals Ax + Az, compared with a small tolerance for rounding.
- Dot product:
  - A known result.
  - Perpendicular vectors give 0.
  - Negative and decimal values work.
  - Swapping the two vectors gives the same answer.
  - Two empty vectors give 0.
  - Vectors of different lengths raise an error.


## LLM Prompt 3

**Prompt**:
Given the current implementation of matvec_multiply function, assess possible failure mode for the functions and add logic to guard them.

**Response**:
Read matvec_multiply.py

Before changing anything, I'll check how the current code behaves on bad inputs.

Write scratchpad/probe.py  (tries each bad input and prints the result or error)
Bash: run probe.py normally and with python3 -O (asserts disabled)

== normal ==
length mismatch        -> AssertionError: vectors must have the same length
None input             -> TypeError: object of type 'NoneType' has no len()
string vectors         -> TypeError: unsupported operand type(s) for +=: 'int' and 'str'
string element         -> TypeError: unsupported operand type(s) for +=: 'int' and 'str'
nested list element    -> TypeError: unsupported operand type(s) for +=: 'int' and 'list'
NaN element            -> returned nan
float overflow         -> returned inf
ragged matrix          -> AssertionError: vectors must have the same length
1-D matrix             -> TypeError: object of type 'int' has no len()
None matrix            -> TypeError: 'NoneType' object is not iterable
column-vector x        -> TypeError: unsupported operand type(s) for +=: 'int' and 'list'
== python -O (asserts disabled) ==
length mismatch        -> returned 5
None input             -> TypeError: object of type 'NoneType' has no len()
string vectors         -> TypeError: unsupported operand type(s) for +=: 'int' and 'str'
string element         -> TypeError: unsupported operand type(s) for +=: 'int' and 'str'
nested list element    -> TypeError: unsupported operand type(s) for +=: 'int' and 'list'
NaN element            -> returned nan
float overflow         -> returned inf
ragged matrix          -> returned [3, 3]
1-D matrix             -> TypeError: object of type 'int' has no len()
None matrix            -> TypeError: 'NoneType' object is not iterable
column-vector x        -> TypeError: unsupported operand type(s) for +=: 'int' and 'list'

The worst problem: when Python runs with -O, assert statements are skipped, so mismatched sizes return wrong answers without any error. NaN and float overflow also pass through unnoticed. I'll add the guards now.

Write matvec_multiply.py

import math
import numbers
import random
import time


# Guard: v must be a list/tuple of finite real numbers
def check_vector(v, name):
    if not isinstance(v, (list, tuple)):
        raise TypeError(f"{name} must be a list or tuple, got {type(v).__name__}")
    for i, val in enumerate(v):
        if not isinstance(val, numbers.Real):
            raise TypeError(f"{name}[{i}] must be a real number, got {type(val).__name__}")
        if not math.isfinite(val):
            raise ValueError(f"{name}[{i}] must be finite, got {val}")


# create a function to compute the dot product of two vectors using a for loop
# add comments for the selected function
def dot_product(a, b, check=True):
    # Validate inputs; matvec_multiply passes check=False after validating once
    if check:
        check_vector(a, "a")
        check_vector(b, "b")
        if len(a) != len(b):
            raise ValueError(f"vectors must have the same length, got {len(a)} and {len(b)}")

    # Sum of element-wise products
    result = 0
    for i in range(len(a)):
        result += a[i] * b[i]

    # Finite floats can still overflow to inf
    if isinstance(result, float) and not math.isfinite(result):
        raise OverflowError("dot product overflowed")
    return result


# create a function to compute the matrix-vector product using the dot_product function
# add comments for the selected function
def matvec_multiply(A, x):
    # Validate A (list of rows) and x once, so dot_product can skip re-checking x per row
    if not isinstance(A, (list, tuple)) or len(A) == 0:
        raise ValueError("A must be a non-empty list of rows")
    check_vector(x, "x")
    for i, row in enumerate(A):
        check_vector(row, f"A[{i}]")
        if len(row) != len(x):
            raise ValueError(f"A[{i}] has {len(row)} columns but x has length {len(x)}")

    # Each entry of the result is the dot product of one row of A with x
    return [dot_product(row, x, check=False) for row in A]


# create a main function to test the matrix-vector product function using randomly generated data of size 1000x1000
# add comments for the selected function
def main():
    # Random 1000x1000 matrix and length-1000 vector
    n = 1000
    A = [[random.random() for _ in range(n)] for _ in range(n)]
    x = [random.random() for _ in range(n)]

    # Time the product and show a sample of the result
    start = time.time()
    y = matvec_multiply(A, x)
    print(f"Output length: {len(y)}, time: {time.time() - start:.3f}s")
    print(f"First 5 entries: {y[:5]}")


if __name__ == "__main__":
    main()

I've added the guards. Next I'll update the tests: two of them expect the old AssertionError, and I'll add a few tests for the new checks.

Edit matvec_tests.py  (replace the raises_assertion helper)

# Check that calling fn(*args) raises the given exception type
def raises(exc, fn, *args):
    try:
        fn(*args)
    except exc:
        return True
    return False

Edit matvec_tests.py  (dimension-mismatch test now expects ValueError; add test_matvec_invalid_inputs)

def test_matvec_dimension_mismatch():
    # Number of columns must equal vector length
    assert raises(ValueError, matvec_multiply, [[1, 2], [3, 4]], [1, 2, 3])


def test_matvec_invalid_inputs():
    # Ragged rows, empty/None/1-D matrix, and a column-vector x are rejected
    assert raises(ValueError, matvec_multiply, [[1, 2], [3]], [1, 1])
    assert raises(ValueError, matvec_multiply, [], [1, 2])
    assert raises(ValueError, matvec_multiply, None, [1, 2])
    assert raises(TypeError, matvec_multiply, [1, 2], [1, 2])
    assert raises(TypeError, matvec_multiply, [[1, 2]], [[1], [2]])

Edit matvec_tests.py  (length-mismatch test now expects ValueError; add test_dot_invalid_inputs)

def test_dot_length_mismatch():
    assert raises(ValueError, dot_product, [1, 2], [1, 2, 3])


def test_dot_invalid_inputs():
    # Non-list inputs, non-numeric entries, NaN/inf, and overflow are rejected
    assert raises(TypeError, dot_product, None, [1])
    assert raises(TypeError, dot_product, "ab", [2, 3])
    assert raises(TypeError, dot_product, ["1", 2], [2, 3])
    assert raises(ValueError, dot_product, [float("nan"), 1], [1, 1])
    assert raises(ValueError, dot_product, [float("inf")], [1])
    assert raises(OverflowError, dot_product, [1e200], [1e200])

Bash: run python3 matvec_tests.py, python3 -O matvec_tests.py, and python3 matvec_multiply.py

== tests ==
PASS test_matvec_known_values
PASS test_matvec_identity
PASS test_matvec_non_square
PASS test_matvec_zero_vector
PASS test_matvec_dimension_mismatch
PASS test_matvec_invalid_inputs
PASS test_matvec_random_linearity
PASS test_dot_known_values
PASS test_dot_orthogonal
PASS test_dot_negative_and_float
PASS test_dot_commutative
PASS test_dot_empty
PASS test_dot_length_mismatch
PASS test_dot_invalid_inputs
All 14 tests passed
== tests with -O ==
All 14 tests passed
== main ==
Output length: 1000, time: 0.400s
First 5 entries: [242.61481021538805, 233.95007635917383, 238.10846843577943, 231.89910948926808, 249.68349117187114]

Bash: re-run probe.py with python3 -O

length mismatch        -> ValueError: vectors must have the same length, got 2 and 3
None input             -> TypeError: a must be a list or tuple, got NoneType
string vectors         -> TypeError: a must be a list or tuple, got str
string element         -> TypeError: a[0] must be a real number, got str
nested list element    -> TypeError: b[0] must be a real number, got list
NaN element            -> ValueError: a[0] must be finite, got nan
float overflow         -> OverflowError: dot product overflowed
ragged matrix          -> ValueError: A[1] has 1 columns but x has length 2
1-D matrix             -> TypeError: A[0] must be a list or tuple, got int
None matrix            -> ValueError: A must be a non-empty list of rows
column-vector x        -> TypeError: x[0] must be a real number, got list

I added input checks to matvec_multiply.py. Every bad input I tried now fails with a clear error instead of a wrong answer. All 14 tests pass, and main() still multiplies the 1000x1000 matrix, taking 0.40s.

How the functions failed before:
- Vectors of different lengths, run with python -O: assert is skipped, so it returned a wrong answer (5) with no error.
- Matrix rows of different lengths, run with python -O: returned a wrong result ([3, 3]) with no error.
- NaN or inf in the input: returned nan with no error.
- Two large floats (1e200 * 1e200): returned inf with no error.
- None, strings, or non-numbers inside the vectors: crashed with unclear messages like "unsupported operand type(s) for +=: 'int' and 'str'".
- Matrix that is None, 1-D, or has a column-vector x: crashed with "has no len()" or "not iterable".

What I added:
- check_vector(v, name): a new helper. It requires a list or tuple of real, finite numbers, and the error names the bad position, e.g. "a[0] must be a real number, got str".
- dot_product:
  - The assert is now a ValueError, so the check still runs under -O.
  - Both vectors are validated.
  - It raises OverflowError if a float result becomes inf.
- matvec_multiply:
  - Rejects an empty or non-list A.
  - Checks x once and each row once, and says which row has the wrong number of columns.
  - It then calls dot_product(row, x, check=False), so x isn't re-checked for all 1000 rows.

I reran all the bad inputs with python3 -O, and each one now raises the expected error.

Changes to matvec_tests.py:
- The two size-mismatch tests expected AssertionError, so I changed the helper to raises(exc, fn, *args).
- I added test_matvec_invalid_inputs and test_dot_invalid_inputs, which cover the cases listed above.

Two things to know:
- NumPy arrays: they are now rejected, since only lists and tuples are accepted. Convert them with .tolist() first.
- -O and the test file: the tests use assert themselves, so run them without -O. The command is still python3 matvec_tests.py.


